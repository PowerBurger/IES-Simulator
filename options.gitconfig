 [merge]d
    tool = unityyamlmerge

    [mergetool "unityyamlmerge"]
    trustExitCode = false
    cmd = '<INSIDE THESE THINGS, WRITE THE PATH TO UNITY YAML.EXE, WHICH IS IN YOU UNITY INSTALLATION. FOR MORE INFO: https://docs.unity3d.com/Manual/SmartMerge.html>' merge -p "$BASE" "$REMOTE" "$LOCAL" "$MERGED"